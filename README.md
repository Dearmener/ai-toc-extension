# AI Chat TOC Sidebar (AI 目录侧边栏助手)

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Platform](https://img.shields.io/badge/platform-Chrome%20%7C%20Edge%20%7C%20Brave-green)

**AI Chat TOC Sidebar** 是一个轻量级的浏览器扩展，专为提高阅读长篇 AI 对话（如 ChatGPT, Gemini, Claude 等）的效率而设计。它会自动扫描页面内容，生成一个悬浮的、可拖拽、可调整大小的目录（Table of Contents），让你在复杂的对话中轻松导航。

---

## ✨ 核心功能

- **📚 自动生成目录**：智能识别 `H1`-`H3` 标题以及 Markdown 中的 **加粗文本**（常被 AI 用作伪标题），生成清晰的层级结构。
- **🎯 双向同步**：
  - 点击目录：平滑滚动到对应段落。
  - 滚动页面：目录自动高亮当前正在阅读的章节。
- **🖐 自由拖拽**：按住顶部标题栏，将目录放置在屏幕任意位置。
- **📐 只有缩放**：
  - **拖动左边缘**：调整侧边栏宽度。
  - **拖动底部边缘**：调整侧边栏高度。
- **💾 状态记忆**：自动保存你的位置、大小和折叠状态，刷新页面后依然保持原样。
- **🌙 现代化 UI**：深色玻璃拟态风格（Glassmorphism），完美融入 ChatGPT 和 Gemini 的深色模式。

---

## 🚀 安装指南

由于这是一个开源工具，目前通过“开发者模式”加载到浏览器中。支持 Chrome、Edge、Brave 等 Chromium 内核浏览器。

### 第一步：准备文件
git克隆或下载本仓库代码到本地电脑。
1. 克隆仓库：
```bash
   git clone https://github.com/yourusername/ai-toc-extension.git
```
2. 或者下载 ZIP 文件并解压到一个文件夹（例如 `ai-toc-extension`）。

### 第二步：加载扩展

1. 打开浏览器（以 Chrome 为例），在地址栏输入并回车：
   `chrome://extensions`
2. 在页面右上角，找到并开启 **“开发者模式” (Developer mode)** 开关。
3. 点击左上角出现的 **“加载已解压的扩展程序” (Load unpacked)** 按钮。
4. 选择你在第一步中下载的文件夹 `ai-toc-extension`。

🎉 **安装完成！** 你现在应该能看到插件图标出现在浏览器工具栏中。

---

## ⚙️ 配置指南 (支持镜像站/Claude)

默认情况下，插件仅在官方 `chatgpt.com` 和 `gemini.google.com` 上生效。如果你使用的是国内镜像站（如 `plusai.io`）或 `Claude.ai`，需要修改 `manifest.json`。

### 如何添加自定义网站

1. 打开你的 `ai-toc-extension` 文件夹。
2. 用文本编辑器打开 `manifest.json`。
3. 找到 `"content_scripts"` -> `"matches"` 数组。
4. 添加你的目标网址。

**示例代码：**

```json
"matches": [
  "https://chatgpt.com/*",
  "https://gemini.google.com/*",
  "https://claude.ai/*",          <-- 添加这一行
  "https://*.plusai.io/*"         <-- 添加镜像站 (使用 * 通配符)
],
```

5. 保存文件。
6. 回到 `chrome://extensions` 页面，找到本插件，点击 **刷新图标 (Reload)**。
7. 刷新你的 AI 对话页面，侧边栏即可出现。

---

## 📖 使用说明

1. **移动位置**：鼠标按住侧边栏顶部的“目录”区域，拖动到屏幕任意位置。
2. **调整宽度**：鼠标移动到侧边栏的**左边缘**，光标变为双向箭头时，按住拖动。
3. **调整高度**：鼠标移动到侧边栏的**下边缘**，光标变为双向箭头时，按住拖动。
4. **折叠/展开**：点击右上角的 `_` 按钮，可以将侧边栏最小化为一个浮动小方块，再次点击即可展开。

---

## 🛠 常见问题排查

**Q: 打开网页后没有看到目录？**
- 确保你已经刷新了该网页。
- 检查网页链接是否已包含在 `manifest.json` 的 `matches` 列表中。
- 确保当前对话中有标题（H1, H2, H3 或加粗文本），如果内容太短，插件可能不会显示。

**Q: 目录高亮不准确？**
- 插件使用了视口检测算法。尝试稍微上下滚动一下页面，或者点击目录项进行校准。

**Q: 插件报错或不工作？**
- 尝试在 `chrome://extensions` 页面点击“错误”按钮查看日志。
- 删除该扩展并重新“加载已解压的扩展程序”。

---

## 📄 License

MIT License. 欢迎 Fork 和提交 PR！
